#include "main.h"

/**
 * _isdigit - Prints a digit from 0 to 9
 *@c : The character to check
 *
 * Return: 1 if digit or 0 if otherwise
 */

int _isdigit(int c)

{
	if (c >= 0 && c <= 9)
	{
	return (1);
	}
	return (0);
}
