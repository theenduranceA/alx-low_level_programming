#include "main.h"

/**
 * sub - Function that sub two integers and returns the result
 * @x: first int
 * @y: int to be subtracted from x
 * Return: result of y from x
 */
int sub(int x, int y)

{
	int z;

	z = x - y;

	return (z);
}
