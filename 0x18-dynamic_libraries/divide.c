#include "main.h"

/**
 * div - Function that divides two integers and returns the result
 * @x: numerator
 * @y: denominator
 * Return: result of x divided by y
 */
int div(int x, int y)

{
	int z;

	z = x / y;

	return (z);
}
